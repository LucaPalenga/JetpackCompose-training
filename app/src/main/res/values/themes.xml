<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.JCEx" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/purple_700</item>
    </style>

    <!-- Base application theme. In this Jetpack Compose sample app, these colors
      don't affect the Composable functions, just the color of the Status Bar.
      For the colors used by the Composable functions, see Theme.kt -->
    <style name="Theme.Jetnews" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/red700</item>
        <item name="colorPrimaryDark">@color/red900</item>
        <item name="colorAccent">@color/red700</item>
        <item name="android:statusBarColor">@color/status_bar</item>
    </style>

    <!--    <style name="Theme.DynamicTheme" parent="Theme.Material3.DayNight" />-->

    <!-- theming compose -->
    <style name="Theme.ComposeTheming" parent="Theme.Material.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/status_bar</item>
    </style>

    <style name="Theme.Material.DayNight.NoActionBar" parent="@android:style/Theme.Material.Light.NoActionBar" />
    <!-- /theming compose -->

    <style name="Theme.Dynamic" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="colorPrimary">@color/teal_200</item>
        <item name="colorSecondary">@color/another_custom_color</item>
    </style>

</resources>